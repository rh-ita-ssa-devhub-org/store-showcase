Node.js Pipeline has been installed.

1. The pipeline will build your Node.js application using the Dockerfile at: {{ .Values.image.dockerfile }}

2. The built image will be pushed to: {{ include "image.dev-url" . }}

3. Git repository: {{ .Values.git.repo }}
   Branch: {{ .Values.git.branch }}

4. A webhook has been configured to trigger builds on git push events.

