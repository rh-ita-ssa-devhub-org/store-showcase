apiVersion: v1
kind: Service
metadata:
  name: {{ include "store-backend.fullname" . }}-service
  labels:
    {{- include "store-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "store-backend.selectorLabels" . | nindent 4 }}

