apiVersion: v1
kind: Service
metadata:
  name: {{ include "store-frontend.fullname" . }}-service
  labels:
    {{- include "store-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "store-frontend.selectorLabels" . | nindent 4 }}

