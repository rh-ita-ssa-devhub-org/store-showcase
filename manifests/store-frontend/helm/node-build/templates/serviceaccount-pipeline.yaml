{{- if eq .Values.image.registry "Quay" }}
kind: ServiceAccount
apiVersion: v1
metadata:
  name: pipeline
secrets:
  - name: {{ include "node-build.name" . }}-registry-creds
imagePullSecrets:
  - name: {{ include "node-build.name" . }}-registry-creds
{{- end }}
